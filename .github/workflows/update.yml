name: Update job listings

on:
  schedule:
    - cron: '0 9 * * *'   # Every day at 09:00 UTC
  workflow_dispatch:       # Also runnable manually from GitHub UI

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write      # Needed to push the updated README

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Regenerate README
        run: python3 scripts/generate_readme.py --repo remote-tech-jobs-europe

      - name: Commit and push if changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git diff --staged --quiet || git commit -m "chore: daily job listings update $(date -u '+%Y-%m-%d')"
          git push
